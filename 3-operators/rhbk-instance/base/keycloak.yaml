apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
spec:
  instances: 1
  db:
    vendor: postgres
    host: postgres-db
    usernameSecret:
      name: keycloakdb-pguser-keycloakdb
      key: user
    passwordSecret:
      name: keycloakdb-pguser-keycloakdb
      key: password
  hostname:
    hostname: https://sso.apps.rosa.grant-pub-prd.0r8r.p3.openshiftapps.com
  ingress:
    className: openshift-default
